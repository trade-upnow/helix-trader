# 留空或删掉：浏览器请求 /api/*（同源），由 Next rewrites 转发到后端（推荐）
# 填完整 URL：浏览器直连后端（需后端 CORS 允许）
NEXT_PUBLIC_API_BASE_URL=
# rewrites 目标，仅服务端使用
BACKEND_PROXY_URL=http://127.0.0.1:8000
NEXT_PUBLIC_ENABLE_INTERNAL_TESTNET=false
